<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Only sideloaded builds may install APK updates. Play builds update
         through Google Play and intentionally do not request this permission. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
</manifest>
